[project]
name = "rag-mcp-server"
version = "0.1.0"
description = "MCP server for retrieving context from a milvus vector database"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.3",
    "pymilvus>=2.5.10",
    "requests>=2.32.3",
]
